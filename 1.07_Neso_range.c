#include<stdio.h>

int main(){
    unsigned char a = -3;
    signed char b =129;

    printf("%d \n%d",a,b);

    //When we give the values exceeding the range of data type the values will repeat with in the range
}
