#include<stdio.h>
int main(){
    int a=32;

    //right shift means shift bit towards right side upto mentioned numbers
    //It is also reprents as number/ 2^n(where n is the number of bits to be shifted)

    printf("%d\n",a>>2);
    //Bit wise & and | are used to off and on the bits throufh assignment operators.

    printf("%d\n",a>>3);

}